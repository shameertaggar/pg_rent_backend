runConfig:
  runtime: nodejs20
  entrypoint: npm start
  minInstances: 1
  maxInstances: 10
  concurrency: 100

build:
  command: "npm install"
  outputDirectory: "."

env:
  - name: JWT_SECRET
    secret: "JWT_SECRET"
  - name: SERVICE_ACCOUNT_KEY
    secret: "SERVICE_ACCOUNT_KEY"
